// TODO: import the projects array from '../data/projects'
import ProjectCard from './ProjectCard'
import './ProjectGrid.css'

function ProjectGrid() {
  // TODO: use .map() to render a ProjectCard for each project
  // Remember: each ProjectCard needs a key prop set to project.id
  return (
    <div className="grid">
      {/* Your mapped cards go here */}
    </div>
  )
}

export default ProjectGrid
